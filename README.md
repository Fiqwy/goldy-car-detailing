# Goldy Car Detailing

Mobile car detailing on the Gold Coast. Static demo site, vanilla stack, no build step.

```
python3 -m http.server 8000
# open http://localhost:8000
```

See `HANDOFF.md` for everything else.
